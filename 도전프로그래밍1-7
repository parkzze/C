//열혈C 도전프로그래밍1-7번문제
#include <stdio.h>
int formul(int n){
	int k=0 ;
	int c = 1;
  
	do{
		c = 2 * c;
		k++;
	} while (2 * c <= n);
	
	printf("공식을 만족하는 k의 최댓값은 %d\n",k);
	return 0;
}
int main(){
	int n;
	printf("2^k=n의 상수 n입력:");
	scanf("%d", &n);
	formul(n);
}
